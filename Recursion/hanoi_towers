#include <iostream>
#include <vector>


using namespace std;

void hanoi_tower(int n, vector<int> start, vector<int> end, vector<int> help) {
    if (n == 0) {
        return;
    }
    hanoi_tower(n - 1, start, help, end);
    start.pop_back();
    end.push_back(n);
    hanoi_tower(n - 1, end, help, start);

}




int main() {
    vector<int> start;
    for (int i = 1; i <= 5; i++)
        start.push_back(i);
    for (int i = 0; i < start.size(); i++)
        cout << start[i];

    vector<int> end;
    vector<int> help;


    hanoi_tower(5, start, end, help);
    for (int i = 0; i < end.size(); i++)
        cout << end[i];

    return 0;
}
